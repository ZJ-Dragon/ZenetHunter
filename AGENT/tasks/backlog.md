# Backlog

These are coordination items worth tracking but not required to finish this branch.

- Validate whether root localized README variants should eventually mirror the full English README instead of only the AI-agent guidance and a pointer back to the main README
- Reconcile the remaining legacy frontend field usage (`attack_status`) with canonical backend contracts in a dedicated compatibility task
- Consider adding AGENT references to `backend/README.md` and other subsystem entrypoints if onboarding continues to drift
- Add a lightweight contract-check list or test coverage for websocket event names and payload shapes
